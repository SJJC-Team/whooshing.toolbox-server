# name: WhooshingService Integration Tests

# on:
#   push:
#     branches: [ develop ]
#   pull_request:
#     branches: [ develop ]

# jobs:
#   integration-tests:
#     runs-on: ubuntu-latest
#     container: swift:6.0
#     steps:
#     - name: Checkout
#       uses: actions/checkout@v3

#     - name: Install lsof
#       run: apt-get update && apt-get install -y lsof

#     - name: Cache Swift dependencies - main
#       uses: actions/cache@v4.2.0
#       with:
#         path: |
#           ~/.swiftpm
#           .build
#         key: ${{ runner.os }}-swift-${{ hashFiles('Package.swift') }}
#         restore-keys: |
#           ${{ runner.os }}-swift-

#     - name: Cache Swift dependencies - server testing
#       uses: actions/cache@v4.2.0
#       with:
#         path: |
#           ~/.swiftpm
#           cd Tests/testing-servers/.build
#         key: ${{ runner.os }}-server-testing-swift-${{ hashFiles('Tests/testing-servers/Package.swift') }}
#         restore-keys: |
#           ${{ runner.os }}-server-testing-swift-

#     - name: Build server
#       run: |
#         cd Tests/testing-servers
#         swift build -c release

#     - name: Start server in background
#       run: |
#         Tests/testing-servers/.build/x86_64-unknown-linux-gnu/release/App &
#         echo $! > server.pid

#     - name: Wait for server to be ready
#       run: |
#         for i in {1..10}; do
#           if lsof -i :6500 > /dev/null; then
#             echo "✅ Server is ready"
#             exit 0
#           else
#             echo "⏳ Waiting for server..."
#             sleep 2
#           fi
#         done
#         echo "❌ Server did not start in time"
#         exit 1

#     - name: Run tests
#       run: |
#         swift build
#         swift test

#     - name: Stop server
#       run: |
#         kill $(cat server.pid)
